<nav class="navbar navbar-inverse">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			</button>
			<%= link_to 'MovieBucketList', root_path, :class => 'navbar-brand' %>
		</div>

		<div id="navbar" class="navbar-collapse collapse">
			<ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to "Your Bucket Lists", user_bucket_lists_path(current_user.id) %></li>
          <li><%= link_to "Create New Bucket List", new_user_bucket_list_path(current_user.id) %></li>
          <li><%= link_to "All Movies", movies_path %></li>
          <li><%= link_to "Highest Rated Movies", highest_rating_path %></li>
          <li><%= link_to "Create New Movie", new_movie_path %></li>
          <li><%= link_to "Movies You've Watched", seen_path %></li>
        <% else %>

        <% end %>
			</ul>
			<ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
					<li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
				<% else %>

				<% end %>
			</ul>
		</div>
	</div>
</nav>
